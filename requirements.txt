Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Bcrypt==1.0.1
Werkzeug==3.0.4
python-dotenv==1.0.1
groq==0.11.0
gunicorn==23.0.0
psycopg2-binary==2.9.9

# --- extraction.py dependencies -------------------------------------------
# extraction.py was not included in what I was given to review, so these are
# inferred from "PDF/document processing" being a stated feature. Confirm
# against extraction.py's actual imports before deploying — swap/add as
# needed (e.g. pypdf vs PyPDF2, add pytesseract + Pillow if it does OCR).
pypdf==4.3.1
python-docx==1.1.2
python-pptx==1.0.2