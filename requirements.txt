Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Bcrypt==1.0.1
Werkzeug==3.0.4
python-dotenv==1.0.1
groq==0.11.0
httpx==0.27.2   # groq 0.11.0 still passes `proxies` to httpx's Client, which
                # newer httpx (0.28+) removed — pin below that or you'll hit
                # "Client.__init__() got an unexpected keyword argument 'proxies'"
gunicorn==23.0.0
psycopg2-binary==2.9.10

# --- extraction.py dependencies -------------------------------------------
# extraction.py was not included in what I was given to review, so these are
# inferred from "PDF/document processing" being a stated feature, plus
# python-pptx (confirmed by the ModuleNotFoundError in your deploy log).
# Still confirm against extraction.py's actual imports — swap/add as needed
# (e.g. pypdf vs PyPDF2, add pytesseract + Pillow if it does OCR).
pypdf==4.3.1
python-docx==1.1.2
python-pptx==1.0.2